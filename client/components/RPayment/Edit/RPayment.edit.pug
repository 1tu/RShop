v-container(text-xs-center)
  v-layout(row wrap)
    v-flex.mb-3(xs12)
      h2 Payment {{ model.id? 'edit' : 'create' }}
    v-flex(xs12)
      v-form(@submit.prevent='submit')
        template(v-if='model')
          //- v-text-field(label='Name' v-model='model.nameFirst' name='nameFirst'
          //-   v-validate='"required|max:20"' :error-messages='errors.collect("nameFirst")')

          v-btn(type='submit') submit
          v-btn( @click='clear') clear
